{
  "name": "browser-agent-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run test:unit && npm run test:integration && npm run test:e2e",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "test:e2e": "playwright test",
    "test:all": "npm run test",
    "check": "npm run build && npm run test",
    "test:ollama": "vitest run tests/smoke",
    "test:replay": "vitest run tests/unit/scenario-* tests/unit/replay-* tests/integration/scenario-replay.integration.test.ts && playwright test tests/e2e/scenario-replay.e2e.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/node": "^24.3.0",
    "playwright": "^1.54.2",
    "typescript": "^5.9.2",
    "vitest": "^2.1.1"
  }
}
